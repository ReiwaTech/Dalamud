# CN update — 2026-09-24

## Sources

- Base: ReiwaTech/Dalamud `cn`, `e79cb69cd9d39af25dfdd301d5e7d9b00678ef34`.
- Merged upstream: goatcorp/Dalamud `master` (its default branch, not `main`), `ea74655e56f90e4519ae5a04a51ccd221f5b70c2`. Assembly version remains upstream's `15.0.3.5`.
- Reference only: ottercorp/Dalamud `master`, `a5745232e9b5796eac79429ec1fd4b8fd9141523`.
- Structs: merged upstream `313161e448e335adddd928f8a0212b2c328b5659` into ReiwaTech's CN `9218c5681dedb2722e8899e580fe3235afa6b372`; selectively ported from ottercorp's `7bbe59afcd0ba7b51cd358b3cbfd2805103f0333`.
- Resulting local structs commit: `25762ad04` on `codex/cn-latest-adaptation`.

## Adaptations

- Retain the existing CN language, fonts, texture paths, runtime location and plugin/repository behavior.
- Retain existing CN signatures for TargetSystem, ContentsFinder, ItemOrderModule and AtkResNode.
- Port CN signatures for XBMManager, AgentLobby, RaptureHotbarModule, RaptureTextModule and AtkUnitBase.
- Update RaptureAtkModule for the latest addon factory layout while retaining its CN `-0x10` field shift; retain the AtkModule shift.
- Port the UIModule shift beginning at InfoModule. **Do not port ottercorp's shift of UI3DModule or RaptureAtkModule**: the current CN client's virtual getters return `+0xBB150` and `+0xD2690`, respectively.
- Port ottercorp's keyboard completion call-site signature. Preserve volatile registers, SIMD registers, flags and Windows x64 shadow space around the managed callback; keep the latest upstream completion implementation and contain callback exceptions.
- Adopt upstream's CMake build migration, retaining CN workflow behavior. Change invalid SDK version `10.0.0` to `10.0.100` with the existing roll-forward policy.

No ottercorp repository/CDN substitutions, plugin manifest/installer policies, extra language overlay packs, launcher bridge, telemetry changes, or self-test bypasses were imported. Existing behavior in the starting CN branch is preserved.

## Validation

Target client: local CN `2026.09.15.0000.0000`.

- Native Release build: CMake 4.3.1, Visual Studio 2026, installed v143 toolset; succeeded.
- Managed Release build and Injector: succeeded.
- Final Dalamud tests: **115 passed, 0 failed, 0 skipped**, including 103 existing tests and 12 new CN layout assertions.
- The new assertions use native virtual getter instructions and live pointer/field checks for UIModule, AtkModule and RaptureAtkModule. They inspect explicit field layout metadata because UIModule cannot be marshaled by the CLR.
- FFXIVClientStructs ResolverTester against the local executable: **2417/2417 signatures resolved (100%)**. This confirms resolution, not correctness of every target or every structure field. The subsequent comparison against upstream `ida/data.yml` is not a CN address oracle.
- Cheat Engine MCP: read-only scans uniquely matched all nine changed/retained CN function signatures and the CN completion call site. Read-only getter disassembly and memory checks established the tested UI offsets. No game memory was patched and the newly built Dalamud was not injected.
- Build warnings include existing nullable/XML/style warnings, native C-linkage warnings, and the upstream SQLitePCLRaw 2.1.2 NuGet advisory.

Local detailed logs: `managed-build.log`, `native-build.log`, `injector-build.log`, `tests.log`, `resolver-validation.log` (ignored build artifacts).

## Publishing and runtime follow-up

Both repository merges are local. Before publishing the parent commit, publish the new FFXIVClientStructs commit to its configured ReiwaTech remote so fresh recursive checkouts can retrieve it. No remote push, deployment, or live replacement was performed.

An in-game smoke test of the newly built binaries is still needed for completion, nameplates, inventory, controller input, and UI events; compilation, signature resolution and selected live layout checks do not establish full runtime compatibility.
